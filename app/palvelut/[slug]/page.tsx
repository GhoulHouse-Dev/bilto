import {notFound} from 'next/navigation';import Link from 'next/link';import {services} from '../../../content/palvelut';export function generateStaticParams(){return services.map(s=>({slug:s.slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=services.find(x=>x.slug===slug);return s?{title:s.title,description:`${s.title} Bilto Oy:ltä Lohjalla ja Uudellamaalla.`}:{}}export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=services.find(x=>x.slug===slug);if(!s)notFound();const schema={"@context":"https://schema.org","@type":"Service","name":s.title,"provider":{"@type":"GeneralContractor","name":"Bilto Oy"},"areaServed":["Lohja","Uusimaa"]};return <main><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(schema)}}/><section className="pageHero"><div className="wrap"><div className="kicker">Palvelut</div><h1>{s.title}</h1><p className="lead muted">{s.description}</p></div></section><section className="section"><div className="wrap"><h2>Suunnitelmasta valmiiksi</h2><div className="process">{['Kartoitus','Suunnittelu','Kustannusarvio','Toteutus','Työnjohto','Luovutus'].map((x,i)=><div className="step" key={x}><span className="kicker">0{i+1}</span><h3>{x}</h3></div>)}</div><div className="actions"><Link className="btn primary" href="/yhteystiedot">Pyydä tarjous</Link></div></div></section></main>}